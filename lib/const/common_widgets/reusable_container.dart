import 'package:flutter/material.dart';

class NextWeekCard extends StatelessWidget {
  const NextWeekCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
